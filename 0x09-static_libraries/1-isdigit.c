#include "main.h"

/**
 * _isdigit: This will  checks for a digit (between 0 and 9)
 * @c: this check int
 * Return: 1 if c is a digit, 0 otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
